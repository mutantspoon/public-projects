ok for the update here do not break the file format, old files have to work, we're adding new data but support both

*  want to put all tools on the right side, 

  * file new save load export should all go at the top,
  * then tools, stack them 2 wide so it's more compressed 
  * then dimensions, view, edit
  * put layers at the bottom of the whole stack
* in edit group and copy paste ,also make hotkeys (mac and pc) work with copy paste
* in select mode lets add a rotate mode, so if you select an object, it puts a ring around it, and you can rotate the object on it's center, snap to 5degree increments
* text should also have a color as well, so when you select text let me change the color to one of the swatches, default to black like you do now
* also give text a size property
